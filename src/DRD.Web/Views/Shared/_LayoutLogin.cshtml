@*
// ============================================================================
// Projet                        	DRD.Web
// Nom du fichier                		_LayoutLogin.cshtml
// Type de fichier                		Vue Razor (Layout)
// Nature C#                     		n/a
// Emplacement                   		Views/Shared
// Auteur                        		Michel Gariépy
// Créé le                       		2025-12-05
//
// Description
//     Layout spécial pour les écrans de connexion DRD (Login / Register).
//     Reproduit fidèlement le rendu visuel DRD v9 : fond pleine page,
//     carte centrée, scripts CDN, Toastr, aucune barre latérale,
//     aucun en-tête.
//
// Fonctionnalité
//     - Layout minimal pour authentification.
//     - Fond d’écran DRD v9.
//     - Toastr (succès/erreur/info/warning).
//     - Scripts CDN Bootstrap/DataTables/Toastr/jQuery.
//     - Section RenderBody() pour Login / Register.
//
// Modifications
//     2025-12-05		Création initiale (port DRD v9 vers DRD v10).
// ============================================================================
*@

@using DRD.Resources.Common

<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>@Common.AppName</title>

    <!-- CSS CDN (identiques DRD v9) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" />

    <!-- CSS DRD -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    <style>
        body {
            background: url('/Images/UNI/backaccceu.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            margin: 0;
        }
    </style>

    @RenderSection("Styles", required: false)
</head>

<body class="d-flex justify-content-center align-items-start pt-5">

    <!-- CONTENU CENTRAL -->
    <main style="width:100%; max-width:600px;">
        @RenderBody()
    </main>

    <!-- SCRIPTS CDN -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

    <!-- TOASTR TempData -->
    <script>
        $(function () {
            toastr.options = {
                closeButton: true,
                progressBar: true,
                positionClass: "toast-top-right",
                timeOut: "5000"
            };

            @if (TempData["ToastrSuccess"] != null)
            {
                    <text>toastr.success("@TempData["ToastrSuccess"]");</text>
            }
            @if (TempData["ToastrError"] != null)
            {
                    <text>toastr.error("@TempData["ToastrError"]");</text>
            }
            @if (TempData["ToastrInfo"] != null)
            {
                    <text>toastr.info("@TempData["ToastrInfo"]");</text>
            }
            @if (TempData["ToastrWarning"] != null)
            {
                    <text>toastr.warning("@TempData["ToastrWarning"]");</text>
            }
        });
    </script>

    @RenderSection("Scripts", required: false)

</body>
</html>
